DATABASE_URL="postgresql://user:password@localhost:5432/clazzo?schema=public"
JWT_SECRET="replace-with-a-long-random-string"
PORT=3001
FRONTEND_ORIGIN="http://localhost:5173"

# Email (OTP login codes + student/staff invite emails), via https://resend.com
# Leave RESEND_API_KEY unset in local dev to log emails to the console instead of sending them.
RESEND_API_KEY=""
RESEND_FROM_EMAIL="Clazzo <onboarding@resend.dev>"
